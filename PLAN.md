# MiniLPA WebUI 改造实施计划

## 📋 项目目标

将 MiniLPA 从 Swing 桌面应用完全重写为基于 Web 的本地服务器应用，提供现代化的 WebUI 界面。

**目标用户**: 单用户本地部署
**技术选型**: Ktor Server (后端) + Svelte (前端)
**语言支持**: 仅中文（简化开发）

---

## 🎯 核心需求

### 功能需求
- ✅ eSIM 配置文件管理（列出、下载、启用、禁用、删除）
- ✅ 配置文件昵称设置
- ✅ 通知管理（列出、处理、删除）
- ✅ 芯片信息查看（EID、内存、证书）
- ✅ 二维码/激活码解析
- ✅ 实时进度显示

### 非功能需求
- ✅ 单用户本地部署（无需认证）
- ✅ 仅监听 localhost（安全性）
- ✅ 启动时自动打开浏览器
- ✅ 跨平台支持（Windows、macOS、Linux）

---

## 🏗️ 技术架构

### 整体架构图

```
┌─────────────────────────────────────────┐
│      浏览器 (http://localhost:8080)      │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │        Svelte Frontend             │ │
│  │  - 配置文件管理页面                 │ │
│  │  - 通知管理页面                     │ │
│  │  - 芯片信息页面                     │ │
│  │  - 设置页面                         │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
         │                        │
    REST API              WebSocket
         │                        │
         ↓                        ↓
┌─────────────────────────────────────────┐
│         Ktor Server (Backend)            │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │     API Routes                     │ │
│  │  - /api/devices                    │ │
│  │  - /api/chip/info                  │ │
│  │  - /api/profiles                   │ │
│  │  - /api/notifications              │ │
│  │  - /ws/progress (WebSocket)        │ │
│  └────────────────────────────────────┘ │
│  ┌────────────────────────────────────┐ │
│  │  Business Logic (复用现有代码)     │ │
│  │  - LPABackend 接口                 │ │
│  │  - LPACExecutor                    │ │
│  │  - 数据模型 (Profile, ChipInfo...) │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│         lpac (命令行工具)                │
│         - profile list                   │
│         - profile download               │
│         - profile enable/disable         │
│         - notification list/process      │
└─────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│    PCSC (PC/SC Smart Card Service)       │
│         - 智能卡读卡器通信               │
└─────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│         eUICC 芯片                       │
└─────────────────────────────────────────┘
```

### 技术栈详情

#### 后端 (Ktor Server)
- **语言**: Kotlin 1.9.x
- **框架**: Ktor Server 3.0.x
- **JVM**: Java 21+
- **核心依赖**:
  - `ktor-server-core` - 核心服务器
  - `ktor-server-cio` - CIO 引擎
  - `ktor-server-websockets` - WebSocket 支持
  - `ktor-server-content-negotiation` - 内容协商
  - `ktor-serialization-kotlinx-json` - JSON 序列化
  - `kotlinx-coroutines-core` - 协程支持
- **端口**: 8080 (默认)
- **监听地址**: 127.0.0.1 (仅本地)

#### 前端 (Svelte)
- **框架**: Svelte 5 + SvelteKit
- **语言**: TypeScript
- **UI 库**: Shadcn-svelte 或 Carbon Components
- **样式**: Tailwind CSS
- **HTTP 客户端**: Fetch API
- **WebSocket**: 原生 WebSocket API
- **构建工具**: Vite
- **语言**: 仅中文

#### 数据格式
- **API 通信**: JSON
- **WebSocket 消息**: JSON
- **配置文件**: YAML (设置)

---

## 📂 项目结构

```
MiniLPA-Web/
├── backend/                      # Ktor Server 后端
│   ├── src/
│   │   └── main/
│   │       └── kotlin/
│   │           └── moe/sekiu/minilpa/
│   │               ├── Application.kt         # 应用入口
│   │               ├── api/                   # REST API 路由
│   │               │   ├── DeviceRoutes.kt
│   │               │   ├── ChipRoutes.kt
│   │               │   ├── ProfileRoutes.kt
│   │               │   └── NotificationRoutes.kt
│   │               ├── ws/                    # WebSocket 端点
│   │               │   └── ProgressWebSocket.kt
│   │               ├── lpa/                   # 业务逻辑 (复用)
│   │               │   ├── LPABackend.kt
│   │               │   ├── LPACExecutor.kt
│   │               │   └── LocalProfileAssistant.kt
│   │               ├── model/                 # 数据模型 (复用)
│   │               │   ├── Profile.kt
│   │               │   ├── ChipInfo.kt
│   │               │   ├── Notification.kt
│   │               │   ├── Device.kt
│   │               │   ├── DownloadInfo.kt
│   │               │   ├── ActivationCode.kt
│   │               │   └── LPACIO.kt
│   │               ├── exception/             # 异常处理 (复用)
│   │               │   └── LPAException.kt
│   │               └── util/                  # 工具类
│   │                   └── BrowserLauncher.kt # 自动打开浏览器
│   ├── resources/
│   │   └── lpac/                 # lpac 二进制文件
│   └── build.gradle.kts          # Gradle 构建配置
│
├── frontend/                     # Svelte 前端
│   ├── src/
│   │   ├── routes/               # SvelteKit 路由
│   │   │   ├── +layout.svelte    # 全局布局
│   │   │   ├── +page.svelte      # 首页（设备选择）
│   │   │   ├── profiles/         # 配置文件页面
│   │   │   │   └── +page.svelte
│   │   │   ├── notifications/    # 通知页面
│   │   │   │   └── +page.svelte
│   │   │   ├── chip/             # 芯片信息页面
│   │   │   │   └── +page.svelte
│   │   │   └── settings/         # 设置页面
│   │   │       └── +page.svelte
│   │   ├── lib/
│   │   │   ├── components/       # 通用组件
│   │   │   │   ├── Navbar.svelte
│   │   │   │   ├── Sidebar.svelte
│   │   │   │   ├── ProgressBar.svelte
│   │   │   │   ├── ProfileCard.svelte
│   │   │   │   ├── NotificationCard.svelte
│   │   │   │   └── Toast.svelte
│   │   │   ├── api/              # API 客户端
│   │   │   │   ├── client.ts     # Fetch 封装
│   │   │   │   └── websocket.ts  # WebSocket 管理
│   │   │   └── stores/           # Svelte Stores
│   │   │       ├── device.ts
│   │   │       ├── profiles.ts
│   │   │       └── progress.ts
│   │   ├── app.html              # HTML 模板
│   │   └── app.css               # 全局样式
│   ├── static/                   # 静态资源
│   │   └── favicon.png
│   ├── svelte.config.js
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   ├── package.json
│   └── tsconfig.json
│
├── docs/                         # 文档
│   ├── API.md                    # API 接口文档
│   ├── DEVELOPMENT.md            # 开发指南
│   └── DEPLOYMENT.md             # 部署说明
│
├── PLAN.md                       # 本文件
└── README.md                     # 项目说明
```

---

## 🔌 API 接口设计

### REST API 端点

#### 1. 设备管理

```http
GET /api/devices
```

**响应示例**:
```json
{
  "devices": [
    {
      "name": "ACS ACR122U",
      "path": "/dev/pcscd/reader0"
    }
  ]
}
```

---

#### 2. 芯片信息

```http
GET /api/chip/info
```

**响应示例**:
```json
{
  "eid": "89000123451234567890123456789012",
  "euiccInfo2": {
    "profileVersion": "3.0.0",
    "euiccFirmwareVer": "1.2.3",
    "extCardResource": {
      "installedApp": 5,
      "freeNonVolatileMemory": 1048576,
      "freeVolatileMemory": 262144
    }
  }
}
```

---

#### 3. 配置文件管理

##### 3.1 列出所有配置文件

```http
GET /api/profiles
```

**响应示例**:
```json
{
  "profiles": [
    {
      "iccid": "8986011234567890123",
      "isdpAid": "A0000005591010FFFFFFFF8900000100",
      "profileState": "enabled",
      "profileNickname": "中国移动",
      "serviceProviderName": "China Mobile",
      "profileName": "China Mobile eSIM",
      "iconType": "PNG",
      "icon": "base64-encoded-image-data",
      "profileClass": "operational"
    }
  ]
}
```

##### 3.2 下载新配置文件

```http
POST /api/profiles
Content-Type: application/json

{
  "activationCode": "1$smdp.example.com$matching-id",
  "confirmationCode": "1234",
  "imei": "123456789012345"
}
```

**响应**: 202 Accepted，进度通过 WebSocket 推送

##### 3.3 启用配置文件

```http
PUT /api/profiles/{iccid}/enable
```

**响应**: 200 OK

##### 3.4 禁用配置文件

```http
PUT /api/profiles/{iccid}/disable
```

**响应**: 200 OK

##### 3.5 设置配置文件昵称

```http
PUT /api/profiles/{iccid}/nickname
Content-Type: application/json

{
  "nickname": "我的工作卡"
}
```

**响应**: 200 OK

##### 3.6 删除配置文件

```http
DELETE /api/profiles/{iccid}
```

**响应**: 200 OK

---

#### 4. 通知管理

##### 4.1 列出所有通知

```http
GET /api/notifications
```

**响应示例**:
```json
{
  "notifications": [
    {
      "seqNumber": 1,
      "profileManagementOperation": "install",
      "notificationAddress": "smdp.example.com",
      "iccid": "8986011234567890123"
    }
  ]
}
```

##### 4.2 处理通知

```http
POST /api/notifications/{seqNumber}/process
```

**响应**: 200 OK

##### 4.3 删除通知

```http
DELETE /api/notifications/{seqNumber}
```

**响应**: 200 OK

---

### WebSocket 端点

#### 实时进度推送

```
WS /ws/progress
```

**服务器推送消息示例**:
```json
{
  "type": "progress",
  "message": "正在下载配置文件...",
  "percent": 45
}
```

```json
{
  "type": "complete",
  "message": "下载完成"
}
```

```json
{
  "type": "error",
  "message": "下载失败: 网络错误"
}
```

---

## 📦 代码复用清单

### ✅ 可直接复用的代码 (约 60%)

| 文件路径 | 复用率 | 说明 |
|---------|-------|------|
| `src/main/kotlin/moe/sekiu/minilpa/model/Profile.kt` | 100% | 配置文件数据模型 |
| `src/main/kotlin/moe/sekiu/minilpa/model/ChipInfo.kt` | 100% | 芯片信息数据模型 |
| `src/main/kotlin/moe/sekiu/minilpa/model/Notification.kt` | 100% | 通知数据模型 |
| `src/main/kotlin/moe/sekiu/minilpa/model/Device.kt` | 100% | 设备数据模型 |
| `src/main/kotlin/moe/sekiu/minilpa/model/DownloadInfo.kt` | 100% | 下载信息模型 |
| `src/main/kotlin/moe/sekiu/minilpa/model/ActivationCode.kt` | 100% | 激活码模型 |
| `src/main/kotlin/moe/sekiu/minilpa/model/LPACIO.kt` | 100% | lpac 通信协议 |
| `src/main/kotlin/moe/sekiu/minilpa/lpa/LPABackend.kt` | 100% | LPA 接口定义 |
| `src/main/kotlin/moe/sekiu/minilpa/lpa/LPACExecutor.kt` | 95% | lpac 执行器（需改造进度回调） |
| `src/main/kotlin/moe/sekiu/minilpa/exception/LPAException.kt` | 100% | 异常定义 |
| `src/main/kotlin/com/github/pgreze/process/*.kt` | 100% | 进程管理工具 |
| `src/main/kotlin/moe/sekiu/minilpa/Tool.kt` | 60% | 工具函数（去除 Swing 依赖部分） |

### ❌ 需要重写的代码 (约 40%)

- `src/main/kotlin/moe/sekiu/minilpa/ui/*.kt` - 所有 Swing UI 代码
- `src/main/kotlin/moe/sekiu/minilpa/Main.kt` - 应用入口
- `src/main/kotlin/moe/sekiu/minilpa/lpa/LocalProfileAssistant.kt` - 业务协调器（需去除 UI 依赖）

---

## 🚀 实施步骤

### Phase 1: 后端基础架构 (5-7 天)

#### 1.1 项目初始化
- [x] 创建 `backend/` 目录
- [x] 设置 Gradle 构建配置
- [x] 添加 Ktor Server 依赖
- [x] 配置 Kotlin 序列化插件

#### 1.2 基础服务器配置
- [x] 创建 `Application.kt` 主入口
- [x] 配置 Ktor 服务器（CIO 引擎）
- [x] 配置端口 8080，监听 127.0.0.1
- [x] 启用 CORS（仅允许 localhost）
- [x] 配置静态资源服务（前端构建产物）

#### 1.3 基础设施
- [x] 配置 JSON 序列化（kotlinx.serialization）
- [x] 配置日志（Logback）
- [x] 实现健康检查端点 `GET /health`
- [x] 实现浏览器自动启动功能

#### 1.4 代码迁移准备
- [x] 复制 `model/` 目录到 backend
- [x] 复制 `lpa/` 目录到 backend（LPABackend, LPACExecutor）
- [x] 复制 `exception/` 目录
- [x] 复制 `com/github/pgreze/process/` 目录
- [x] 复制 lpac 二进制文件到 `resources/lpac/`

---

### Phase 2: 核心 API 实现 (7-10 天)

#### 2.1 设备管理 API
- [x] `DeviceRoutes.kt` - 路由定义
- [x] `GET /api/devices` - 获取设备列表
- [x] 设备检测逻辑（调用 lpac chip info）

#### 2.2 芯片信息 API
- [x] `ChipRoutes.kt` - 路由定义
- [x] `GET /api/chip/info` - 获取芯片信息
- [x] 调用 `LPACExecutor.chipInfo()`

#### 2.3 配置文件管理 API
- [x] `ProfileRoutes.kt` - 路由定义
- [x] `GET /api/profiles` - 列出配置文件
- [x] `POST /api/profiles` - 下载配置文件（基础实现）
- [x] `PUT /api/profiles/:iccid/enable` - 启用配置文件
- [x] `PUT /api/profiles/:iccid/disable` - 禁用配置文件
- [x] `PUT /api/profiles/:iccid/nickname` - 设置昵称
- [x] `DELETE /api/profiles/:iccid` - 删除配置文件
- [ ] 异步操作处理（下载任务）- **进行中**

#### 2.4 通知管理 API
- [x] `NotificationRoutes.kt` - 路由定义
- [x] `GET /api/notifications` - 列出通知
- [x] `POST /api/notifications/:seqNumber/process` - 处理通知
- [x] `DELETE /api/notifications/:seqNumber` - 删除通知

#### 2.5 WebSocket 进度推送
- [x] `ProgressWebSocket.kt` - WebSocket 端点
- [x] `WS /ws/progress` - WebSocket 连接
- [ ] 改造 `LPACExecutor` 进度回调机制 - **进行中**
- [x] 实现进度消息广播（ProgressBroadcaster）
- [ ] 错误处理和重连逻辑

#### 2.6 二维码/激活码解析
- [ ] `POST /api/qrcode/parse` - 上传二维码图片
- [ ] 集成 ZXing 解析二维码
- [ ] 激活码格式验证

---

### Phase 3: 前端开发 (10-14 天)

#### 3.1 项目初始化 (1 天)
- [x] 创建 `frontend/` 目录
- [x] 初始化 SvelteKit 项目
- [x] 配置 TypeScript
- [x] 配置 Tailwind CSS
- [x] 安装 UI 组件库（Carbon Components Svelte）
- [x] 配置开发代理（转发 API 请求到 8080）

#### 3.2 通用组件开发 (2-3 天)
- [x] `Navbar.svelte` - 顶部导航栏
- [x] `ProgressBar.svelte` - 进度条组件
- [x] `Toast.svelte` - 通知提示组件
- [x] `+layout.svelte` - 全局布局
- [x] `ProfileCard.svelte` - 配置文件卡片组件
- [x] `DownloadDialog.svelte` - 下载对话框组件

#### 3.3 API 客户端 (1 天)
- [x] `lib/api/client.ts` - Fetch API 封装
- [x] `lib/api/websocket.ts` - WebSocket 管理
- [x] 请求拦截器和错误处理
- [x] TypeScript 类型定义

#### 3.4 状态管理 (1 天)
- [x] `lib/stores/device.ts` - 设备状态
- [x] `lib/stores/profiles.ts` - 配置文件状态
- [x] `lib/stores/notifications.ts` - 通知状态
- [x] `lib/stores/progress.ts` - 进度状态

#### 3.5 页面开发 (5-7 天)

##### 设备选择页面
- [x] `routes/+page.svelte` - 首页
- [x] 设备列表展示
- [x] 设备选择和连接
- [x] 芯片信息展示

##### 配置文件管理页面（核心）
- [x] `routes/profiles/+page.svelte`
- [x] `lib/components/ProfileCard.svelte` - 配置文件卡片
- [x] `lib/components/DownloadDialog.svelte` - 下载对话框
- [x] 配置文件列表展示
- [x] 配置文件启用/禁用切换
- [x] 配置文件删除确认
- [x] 昵称编辑
- [x] 下载新配置文件对话框
- [x] 二维码上传和解析
- [x] 激活码输入
- [x] 实时进度显示（集成 WebSocket）

##### 通知管理页面
- [x] `routes/notifications/+page.svelte`
- [x] 通知列表展示
- [x] 通知处理按钮
- [x] 通知删除按钮

##### 芯片信息页面
- [x] `routes/chip/+page.svelte`
- [x] EID 展示和复制
- [x] 芯片信息展示（固件版本、内存等）
- [x] 技术详情展示

#### 3.6 WebSocket 集成 (1-2 天)
- [x] 连接管理（自动重连）
- [x] 进度消息接收和状态更新
- [x] 错误处理
- [x] UI 进度条联动

#### 3.7 样式和优化 (1 天)
- [x] 响应式布局
- [x] Carbon Design System 主题（g100 暗色主题）
- [x] 动画和过渡效果
- [x] 性能优化

---

### Phase 4: 集成与优化 (3-5 天)

#### 4.1 前后端联调 (2 天)
- [ ] API 接口调试
- [ ] WebSocket 连接测试
- [ ] 错误处理验证

#### 4.2 完整流程测试 (2 天)
- [ ] 设备检测和选择
- [ ] 配置文件下载流程
- [ ] 配置文件启用/禁用
- [ ] 配置文件删除
- [ ] 通知处理
- [ ] 二维码解析

#### 4.3 用户体验优化 (1 天)
- [ ] 加载状态优化
- [ ] 错误提示优化
- [ ] 操作确认对话框
- [ ] 成功提示反馈

---

### Phase 5: 测试与文档 (2-3 天)

#### 5.1 功能测试 (1 天)
- [ ] 下载配置文件
- [ ] 启用配置文件
- [ ] 禁用配置文件
- [ ] 删除配置文件
- [ ] 通知处理
- [ ] 二维码解析
- [ ] 错误场景测试

#### 5.2 文档编写 (1-2 天)
- [ ] `README.md` - 项目说明
- [ ] `docs/API.md` - API 接口文档
- [ ] `docs/DEVELOPMENT.md` - 开发指南
- [ ] `docs/DEPLOYMENT.md` - 部署说明

#### 5.3 打包和发布 (可选)
- [ ] 前端构建脚本
- [ ] 后端打包配置
- [ ] 创建启动脚本
- [ ] 跨平台测试

---

## ⏱️ 时间估算

| 阶段 | 任务 | 预计时间 |
|-----|------|---------|
| Phase 1 | 后端基础架构 | 5-7 天 |
| Phase 2 | 核心 API 实现 | 7-10 天 |
| Phase 3 | 前端开发 | 10-14 天 |
| Phase 4 | 集成与优化 | 3-5 天 |
| Phase 5 | 测试与文档 | 2-3 天 |
| **总计** | | **27-39 天** |

**全职开发**: 约 1-1.5 个月
**兼职开发**: 约 2-3 个月

---

## 🎯 里程碑

### Milestone 1: 后端可用 (Day 12-17)
- ✅ Ktor Server 运行
- ✅ 所有 REST API 实现完成
- ✅ WebSocket 进度推送可用
- ✅ lpac 调用正常

**验证标准**: 使用 Postman/curl 可以调用所有 API

### Milestone 2: 前端 MVP (Day 22-31)
- ✅ 所有页面基本可用
- ✅ 配置文件管理功能完整
- ✅ WebSocket 实时进度显示
- ✅ 基本样式完成

**验证标准**: 可以完成完整的配置文件下载和管理流程

### Milestone 3: 生产就绪 (Day 27-39)
- ✅ 所有功能测试通过
- ✅ 错误处理完善
- ✅ 文档齐全
- ✅ 跨平台验证

**验证标准**: 可以在 Windows/macOS/Linux 上正常运行

---

## 🚧 技术难点与解决方案

### 1. lpac 进程管理
**难点**: 需要可靠地启动、监控和终止 lpac 进程

**解决方案**:
- 复用现有 `com.github.pgreze.process` 包
- 实现进程超时机制
- 实现进程清理逻辑（服务器关闭时）

### 2. 实时进度推送
**难点**: lpac 的输出需要实时推送到浏览器

**解决方案**:
```kotlin
// 改造 LPACExecutor
suspend fun downloadProfile(
    code: ActivationCode,
    onProgress: suspend (String, Int) -> Unit
): Profile {
    val process = ProcessBuilder(lpacPath, "profile", "download", ...)
        .redirectOutput(ProcessBuilder.Redirect.PIPE)
        .start()

    process.inputStream.bufferedReader().useLines { lines ->
        lines.forEach { line ->
            val lpacio = json.decodeFromString<LPACIO>(line)
            if (lpacio.type == LPACIO.Type.PROGRESS) {
                onProgress(lpacio.payload.lpa.message, calculatePercent())
            }
        }
    }
}

// WebSocket 端点
routing {
    webSocket("/ws/progress") {
        backend.downloadProfile(code) { message, percent ->
            send(Frame.Text(json.encodeToString(Progress(message, percent))))
        }
    }
}
```

### 3. 并发安全（单用户简化）
**难点**: 理论上同一时间只能有一个 lpac 操作

**解决方案**:
- 单用户部署：无需复杂锁机制
- 前端禁用按钮（操作进行中）
- 后端简单标志位检查（可选）

```kotlin
object LPAOperationLock {
    private var isLocked = false

    suspend fun <T> withLock(block: suspend () -> T): T {
        if (isLocked) throw LPAException("操作正在进行中")
        isLocked = true
        try {
            return block()
        } finally {
            isLocked = false
        }
    }
}
```

### 4. 二维码解析
**难点**: 需要在服务器端解析上传的二维码图片

**解决方案**:
```kotlin
post("/api/qrcode/parse") {
    val multipart = call.receiveMultipart()
    multipart.forEachPart { part ->
        if (part is PartData.FileItem) {
            val bytes = part.streamProvider().readBytes()
            val image = ImageIO.read(ByteArrayInputStream(bytes))

            // 使用 ZXing 解析
            val source = BufferedImageLuminanceSource(image)
            val bitmap = BinaryBitmap(HybridBinarizer(source))
            val reader = QRCodeReader()
            val result = reader.decode(bitmap)

            val code = ActivationCode.parse(result.text)
            call.respond(code)
        }
    }
}
```

### 5. 自动打开浏览器
**难点**: 跨平台打开默认浏览器

**解决方案**:
```kotlin
object BrowserLauncher {
    fun openBrowser(url: String) {
        val os = System.getProperty("os.name").lowercase()
        val runtime = Runtime.getRuntime()

        when {
            os.contains("win") -> runtime.exec("rundll32 url.dll,FileProtocolHandler $url")
            os.contains("mac") -> runtime.exec("open $url")
            os.contains("nix") || os.contains("nux") -> runtime.exec("xdg-open $url")
        }
    }
}

// 在服务器启动后调用
fun main() {
    embeddedServer(CIO, port = 8080, host = "127.0.0.1") {
        // ... 配置
    }.start(wait = false)

    BrowserLauncher.openBrowser("http://localhost:8080")

    // 保持服务器运行
    Thread.currentThread().join()
}
```

---

## 🔒 安全考虑

### 单用户本地部署安全措施
1. **仅监听 localhost** (127.0.0.1)
   - 不暴露到局域网或公网
2. **无需认证**
   - 简化用户体验
   - 依赖操作系统的用户隔离
3. **CORS 限制**
   - 仅允许 localhost 源
4. **文件上传限制**
   - 限制二维码图片大小（< 10MB）
   - 限制文件类型（仅图片）

---

## 📊 代码量估算

| 模块 | 文件数 | 代码行数 |
|-----|-------|---------|
| 后端 - API 路由 | 5-8 个 | 500-800 行 |
| 后端 - WebSocket | 1-2 个 | 100-200 行 |
| 后端 - 工具类 | 2-3 个 | 150-250 行 |
| 后端 - 主入口 | 1 个 | 100-150 行 |
| 前端 - 通用组件 | 10-15 个 | 800-1200 行 |
| 前端 - 页面 | 5-6 个 | 1000-1500 行 |
| 前端 - API 客户端 | 2-3 个 | 300-500 行 |
| 前端 - 状态管理 | 3-5 个 | 200-400 行 |
| **总计** | **29-43 个** | **3150-5000 行** |

**复用现有代码**: 约 4000 行
**新增代码**: 约 3000-5000 行
**总代码量**: 约 7000-9000 行

---

## 🎨 UI 设计参考

### 布局结构
```
┌─────────────────────────────────────────┐
│  MiniLPA                      [设置] [?] │ ← Navbar
├────────┬────────────────────────────────┤
│ 设备   │                                │
│ 配置文件│  ┌──────────────────────────┐ │
│ 通知   │  │                          │ │
│ 芯片信息│  │      主内容区域          │ │
│        │  │                          │ │
│        │  │                          │ │
└────────┴──┴──────────────────────────┘─┘
  ↑ Sidebar      ↑ 主要内容
```

### 配置文件卡片设计
```
┌─────────────────────────────────────┐
│ 📱 中国移动              [启用] [⋮] │
│ China Mobile eSIM                   │
│ ICCID: 8986011234567890123         │
│ 状态: 已启用                        │
└─────────────────────────────────────┘
```

### 进度显示
```
┌─────────────────────────────────────┐
│ 正在下载配置文件...                  │
│ ████████████░░░░░░░░ 60%           │
│ 预计剩余时间: 30 秒                  │
└─────────────────────────────────────┘
```

---

## 🧪 测试策略

### 后端测试
- **单元测试**: 核心业务逻辑
- **集成测试**: API 端点
- **工具**: Ktor Test, MockK

### 前端测试
- **组件测试**: 通用组件
- **E2E 测试**: 关键流程（可选）
- **工具**: Vitest, Playwright

---

## 📝 文档清单

### 必需文档
1. **README.md**
   - 项目简介
   - 快速开始
   - 功能特性
   - 系统要求
   - 许可证

2. **API.md**
   - 所有 REST API 端点
   - WebSocket 协议
   - 请求/响应示例
   - 错误码

3. **DEVELOPMENT.md**
   - 开发环境搭建
   - 项目结构说明
   - 构建和运行
   - 调试技巧

4. **DEPLOYMENT.md**
   - 部署前准备
   - 构建步骤
   - 运行说明
   - 故障排查

---

## 🚀 快速开始（预期）

### 用户使用流程

1. **下载并解压**
   ```bash
   unzip MiniLPA-Web-v2.0.0.zip
   cd MiniLPA-Web
   ```

2. **启动服务器**
   ```bash
   ./start.sh         # Linux/macOS
   start.bat          # Windows
   ```

3. **自动打开浏览器**
   - 浏览器自动打开 `http://localhost:8080`
   - 选择设备并开始使用

4. **停止服务器**
   - 关闭浏览器标签页
   - 终端按 `Ctrl+C` 停止服务器

---

## 🔄 后续扩展可能性

### 可选功能（未来版本）
- [ ] 多语言支持（英文、日文、德文）
- [ ] 暗色主题
- [ ] 配置文件导入/导出
- [ ] 操作历史记录
- [ ] 远程 LPA 支持（WebSocket 客户端模式）
- [ ] 多设备管理（如果硬件支持）
- [ ] 配置文件分组和标签
- [ ] 统计和分析功能

---

## 📞 支持与反馈

- **GitHub Issues**: [项目地址]/issues
- **文档**: [项目地址]/docs

---

## 📄 许可证

与原 MiniLPA 项目保持一致。

---

## 👥 贡献者

- 原 MiniLPA 项目作者
- WebUI 改造: [待填写]

---

**最后更新**: 2025-12-03